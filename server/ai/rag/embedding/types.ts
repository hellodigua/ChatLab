/**
 * Embedding service type definitions (server-side)
 * Re-exports from parent types for convenience.
 */

export type { IEmbeddingService, EmbeddingResult, EmbeddingConfig, EmbeddingServiceConfig } from '../types.js'
