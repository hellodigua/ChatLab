/**
 * Vector store type definitions (server-side)
 * Re-exports from parent types for convenience.
 */

export type { IVectorStore, VectorSearchResult, VectorStoreStats, VectorStoreConfig } from '../types.js'
